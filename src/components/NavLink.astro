---

---

<a href="" class="root variant-blur"><span></span><slot /></a>

<style>
  .root {
    position: relative;
    padding-inline: 2.5ch;
    display: inline-flex;

    font-size: 14px;
    line-height: 3.5em;
    font-weight: 700;
    color: rgb(var(--color-text));

    text-transform: uppercase;

    transition:
      background-color var(--trans-default),
      color var(--trans-default),
      text-shadow var(--trans-steps);
  }

  .root.variant-blur::after {
    opacity: 0;
    position: absolute;
    inset: 0;
    content: '';
    background: linear-gradient(
      180deg,
      rgba(239, 129, 27, 0) 47%,
      rgba(239, 129, 27, 0.25) 100%
    );
    transition: opacity var(--trans-default);
  }
  .root.variant-blur:hover::after {
    opacity: 1;
  }

  .root.variant-oval {
    background-color: rgb(var(--color-bg));
    box-shadow: inset 0 0 0 2px rgb(var(--color-bg));
  }

  .root:hover {
    /* background-color: transparent; */
    color: rgb(var(--color-accent));
    text-shadow:
      -3px -3px rgb(var(--color-accent), 20%),
      2.5px 2.5px rgb(var(--color-white), 20%);
  }

  /* .root::after,
  .root:first-child::before {
    bottom: 0;
    content: '';
    position: absolute;
    background: linear-gradient(
      180deg,
      rgba(69, 72, 87, 0) 17.61%,
      #575969 100%
    );
    width: 1px;
  }

  .root::after {
    right: -1px;
    height: 100%;
  }
  .root:first-child::before {
    left: -1px;
  }
  .root:nth-last-child(2)::after,
  .root:first-child::after {
    height: 90%;
  }
  .root:last-child::after,
  .root:first-child::before {
    height: 80%;
  } */

  .root.variant-blur::before,
  .root.variant-blur:first-of-type span {
    bottom: -2px;

    content: '';
    position: absolute;
    background: linear-gradient(
      180deg,
      rgba(69, 72, 87, 0) 17.61%,
      #575969 100%
    );
    width: 1px;
    height: 100%;
  }
  .root.variant-blur::before {
    right: -3px;
  }
  .root.variant-blur:first-of-type span {
    left: -3px;
  }
</style>
